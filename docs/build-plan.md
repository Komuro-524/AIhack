# 実装の進め方と提出前チェック

> `manabi-no-wa/DESIGN.md` から作業メモの部分だけをこちらに移したもの。
> **設計の正は DESIGN.md。ここは手順とチェックリスト。**

---

## 🌙 今夜（9/20）のゴール

> **トランスクリプトを1本食わせたら 知見カードとタグがDBに入る**

ここまで行けば、明日は 🎪B に集中できる。

| | やること | めやす | 完了の基準 |
|:--:|---|---|---|
| 0 | OrcaRouter のキーを3本発行 ＋ Named Router を作る | 20分 | 3本とも `.env.local` に入っている |
| 1 | **テーブル ＋ RLS ＋ チェック**（1工程） | 1.5h | 下の8項目が全部通る |
| 2 | 画面モック（小室さん・**1が終われば並行**） | — | 画面遷移が動く |
| 3 | 🎙️**A だけ**作る | 2h | ライブ1本からカードとタグが出る |
| 4 | 稼働確認 | 30分 | 上のゴールの1文が通る |

> ⚠️ **RLS は後付けできない。** テーブルだけ先に作って後から足すと、書いたクエリが全部通らなくなる。
> 「テーブル完成」の定義に **RLSとチェックまで** を入れること。

---

## 📅 明日（9/21〜22）

| | やること | 完了の基準 | 撮る証拠 |
|:--:|---|---|---|
| 5 | 🎪B を作る | 判断ログが出て 打診が飛ぶ | 📸 判断ログ／企ての再開 |
| 6 | つなぐ（通し確認） | デモシナリオが1周通る | 📸 通し動画 |
| 7 | 壊して直す | モデル落ち・二重投入・巨大入力 | 📸 復旧の動画 |
| 8 | 🔍C（余裕があれば） | ボタン → 数枚撮る → タグ候補 | 📸 |
| 9 | 記事を書く | 5軸に沿った見出しが埋まっている | — |
| 10 | **14:00 提出** | — | — |

> 提出期限は **9/22 15:00**。**14:00 を目標**にして1時間の余裕を持つ。

---

## 📸 撮る証拠（工程の完了条件として撮る。あとでまとめて撮らない）

| 軸 | 撮るもの | いつ |
|---|---|---|
| ① セキュリティ | 下のチェック8項目の画面 | 工程1 |
| ① | チャットに攻撃文を貼って止まる画面 | 工程5 |
| ① | `tags` の `rejected` 一覧（AIが自分で弾いた語） | 工程3 |
| ② コスパ | Request Logs の**モデル別レシート**（A と B で別々に） | 工程3・5 |
| ② | 「全部を高級モデルで回した場合」との比較 | 工程7 |
| ③ 堅牢性 | 誤変換トランスクリプトを食わせて止まる画面 | 工程3 |
| ③ | **処理の途中で殺して再実行 → 続きから再開** | 工程7 |
| ③ | 同じライブを2回投入 → 何も増えない | 工程7 |
| ④ 自律性 | 判断ログ「◯回観測して 立てたのは◯回」 | 工程5 |
| ④ | 企ての履歴まるごと | 工程6 |

---

## ✅ 提出前チェック（5分）

- [ ] anon key で他人の非公開タグ・候補タグ・企てを `select` して **全部0件**
- [ ] `update users set role='admin'` が **弾かれる**
- [ ] エージェントのURLを素で叩いて **401**
- [ ] Supabase のテーブル一覧に **RLS の赤い警告がない**
- [ ] ビルド後のJSを `sk-orca` と `service_role` で検索して **0件**
- [ ] 別の `user_id` で `messages` に `insert` して **弾かれる**
- [ ] 参加していないライブの `messages` を `select` して **0件**（知見カードは見える）
- [ ] `.env` がコミットされていない ／ `docs/evidence/` のスクショに鍵と実名が写っていない

> この8枚のスクショが **そのまま①の証拠になる。**
> 「攻撃を止めた画面」を別に用意しなくても、点検した記録が証拠として成立する。

---

## 📝 記事の目次（5軸に合わせる）

| 章 | 見出し | 中に置くもの |
|:--:|---|---|
| 0 | 誰のどの30分が消えるのか | 痛みの具体。場を1回立てる手間の内訳 |
| 1 | 会議で喋るだけでAIは乗っ取れる | 攻撃が止まる画面／権限分離／弾いた語の一覧 |
| 2 | 2体に割ったら請求書も割れた | モデル別レシート／高級モデル全振りとの比較 |
| 3 | 9日間走るエージェントを途中で殺した | 企ての再開／二重投入／誤変換／Fallback |
| 4 | 渡したのは作業ではなく役割 | 判断ログ／企ての履歴／人の門3つの図 |
| 5 | 聞ける相手が見つかる | 4層の輪／断れる設計／**作らなかったもの** |
| 6 | OrcaRouter をどう使ったか | Named Router／Union Alpha／OrcaReplay |

> 記事は **OrcaRouter と AI HACK に触れると good**。6章を独立させておくと確実。

---

*最終更新: 2026-09-20*
